function cookingByNumbersBetter(number, command1, command2, command3, command4, command5){
 number = Number(number);
  const comandList = {}
}
cookingByNumbersBetter('32', 'chop', 'chop', 'chop', 'chop', 'chop')